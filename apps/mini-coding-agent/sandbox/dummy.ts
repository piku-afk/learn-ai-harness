// this is a dummy file. it is created to test the read_file function of mini-coding-agent harness.
console.log('hello agent');

function add(a: number, b: number): number {
  return a + b;
}

function subtract(a: number, b: number): number {
  return a - b;
}
